let letra = prompt("Digite uma letra:");

if (
    letra === "a" || letra === "e" || letra === "i" ||
    letra === "o" || letra === "u"
) {
    alert("É uma vogal");
} else {
    alert("Não é uma vogal");
}
